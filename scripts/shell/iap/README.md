# IAP

Scripts for establishing an IAP tunnel to either postgres or rmq instance.
The script waits for user input then filters instances by the received input and then figures out the rest of information necessary for the connection and establishes the tunnel.
